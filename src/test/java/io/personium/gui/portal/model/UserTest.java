package io.personium.gui.portal.model;


public class UserTest {
	/**
	 * Check all exposed instantiation and their correctness
	 */
	/*@Test
	public void testInstantiation() {
		User user = new User(1, "HCL", null, null, "HCLT",
				null, null, "1", "All", null, null, null, null, null, null, null, null, new Organization());
		assertTrue("Instantiate Successful", user!=null);
	}*/
	
	/**
	 * Check all exposed instantiation and their correctness
	 */
	/*@Test
	public void testValues() {
		User user = new User(1, "HCL", null, null, "HCLT",
				null, null, "1", "All", null, null, null, null, null, null, null, null, new Organization());
		assertTrue(user.getUserid()==1);
		assertTrue(user.getName().equals("HCL"));
		assertTrue(user.getEncryptedPassphrase()==null);
		assertTrue(user.getSalt()==null);
		assertTrue(user.getFirstName().equals("HCLT"));
		assertTrue(user.getFamilyName()==null);
		assertTrue(user.getEmail()==null);
		assertTrue(user.getStatus().equals("1"));
		assertTrue(user.getPrivilege().equals("All"));
		assertTrue(user.getCreatedAt()==null);
		assertTrue(user.getVerificationSendAt()==null);
		assertTrue(user.getVerifiedAt()==null);
		assertTrue(user.getUpdatedAt()==null);
		assertTrue(user.getLastLoginAt()==null);
		assertTrue(user.getPasswordResetAt()==null);
		assertTrue(user.getVerifiedAt()==null);
		assertTrue(user.getUserEnvironments()==null);
		assertTrue(user.getOrganization()!=null);
	}*/
}
